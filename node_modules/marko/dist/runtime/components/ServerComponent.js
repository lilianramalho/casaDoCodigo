"use strict";

var complain;

class ServerComponent {
  constructor(id, input, out, typeName, customEvents, scope) {
    this.id = id;
    this.S_ = customEvents;
    this.L_ = scope;
    this.typeName = typeName;
    this.R_ = undefined; // Used to keep track of bubbling DOM events for components rendered on the server
    this._V_ = 0;

    this.onCreate(input, out);
    this._W_ = this.onInput(input, out) || input;
    if (this.U_ === undefined) {
      this.U_ = this._W_;
    }
    this.onRender(out);
  }

  set input(newInput) {
    this.U_ = newInput;
  }

  get input() {
    return this.U_;
  }

  set state(newState) {
    this.N_ = newState;
  }

  get state() {
    return this.N_;
  }

  get _v_() {
    return this.N_;
  }

  elId(nestedId) {
    var id = this.id;

    if (nestedId == null) {
      return id;
    } else {
      if (typeof nestedId !== "string") {

        nestedId = String(nestedId);
        // eslint-disable-next-line no-constant-condition
      }

      if (nestedId.indexOf("#") === 0) {
        id = "#" + id;
        nestedId = nestedId.substring(1);
      }

      return id + "-" + nestedId;
    }
  }

  onCreate() {}
  onInput() {}
  onRender() {}
}

ServerComponent.prototype.getElId = ServerComponent.prototype.elId;

module.exports = ServerComponent;