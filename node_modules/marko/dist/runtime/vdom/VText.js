var VNode = require("./VNode");
var inherit = require("raptor-util/inherit");

function VText(value, ownerComponent) {
  this.bE_(-1 /* no children */, ownerComponent);
  this.bF_ = value;
}

VText.prototype = {
  bZ_: true,

  bG_: 3,

  bD_: function (doc) {
    return doc.createTextNode(this.bF_);
  },

  __: function () {
    return new VText(this.bF_);
  }
};

inherit(VText, VNode);

module.exports = VText;