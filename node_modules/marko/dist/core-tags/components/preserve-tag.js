var ComponentsContext = require("../../runtime/components/ComponentsContext");
var getComponentsContext = ComponentsContext.s_;

module.exports = function render(input, out) {
  var shouldPreserve = Boolean(!("if" in input) || input["if"]);
  var ownerComponentDef = out.m_;
  var ownerComponent = ownerComponentDef.n_;
  var key = out.o_;

  out.q_(key, ownerComponent, true);

  if (input.renderBody) {
    var componentsContext = getComponentsContext(out);
    var parentPreserved = componentsContext.t_;
    componentsContext.t_ = parentPreserved || shouldPreserve;
    input.renderBody(out);
    componentsContext.t_ = parentPreserved;
  }

  out.r_();
};